# =============================================================================
# B-TRUST MICROBANKING SYSTEM - ENVIRONMENT CONFIGURATION
# =============================================================================

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION
# -----------------------------------------------------------------------------
PORT=5000
NODE_ENV=development

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
DB_USER=postgres
DB_HOST=localhost
DB_NAME=microbanking
DB_PASSWORD=your_password_here
DB_PORT=5432

# Full connection string (alternative to individual settings)
# DATABASE_URL=postgresql://user:password@localhost:5432/microbanking

# -----------------------------------------------------------------------------
# JWT AUTHENTICATION
# -----------------------------------------------------------------------------
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=24h

# -----------------------------------------------------------------------------
# INTEREST SCHEDULER CONFIGURATION
# -----------------------------------------------------------------------------

# DEBUG MODE: Run schedulers every minute for testing (DISABLE IN PRODUCTION!)
# Set to 1 to enable, 0 or remove to disable
INTEREST_CRON_DEBUG=0

# FD Interest Processing Schedule (Cron format)
# Default: '0 3 * * *' = Every day at 3:00 AM
# Examples:
#   '0 3 * * *'     - Every day at 3:00 AM
#   '*/5 * * * *'   - Every 5 minutes (testing)
#   '0 */6 * * *'   - Every 6 hours
FD_INTEREST_CRON=0 3 * * *

# Savings Interest Processing Schedule (Cron format)
# Default: '30 3 * * *' = Every day at 3:30 AM
SAVINGS_INTEREST_CRON=30 3 * * *

# -----------------------------------------------------------------------------
# LOGGING CONFIGURATION
# -----------------------------------------------------------------------------

# Suppress general application logs (HTTP requests, etc.)
# Interest logs (with emojis) are ALWAYS shown
# Set to 1 to suppress, 0 to show all logs
SUPPRESS_GENERAL_LOGS=0

# Log level: silent, error, warn, info, debug
# Default: info
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# CORS CONFIGURATION (if needed)
# -----------------------------------------------------------------------------
# CORS_ORIGIN=http://localhost:3000

# -----------------------------------------------------------------------------
# NOTES
# -----------------------------------------------------------------------------
# 
# PRODUCTION SETTINGS:
# - INTEREST_CRON_DEBUG=0 (CRITICAL!)
# - NODE_ENV=production
# - SUPPRESS_GENERAL_LOGS=1 (optional, for cleaner logs)
# - LOG_LEVEL=info or warn
# - Use strong JWT_SECRET
#
# DEVELOPMENT/TESTING SETTINGS:
# - INTEREST_CRON_DEBUG=1 (runs every minute)
# - SUPPRESS_GENERAL_LOGS=0 (see all logs)
# - LOG_LEVEL=debug
#
# CRON FORMAT: minute hour day month weekday
# Examples:
#   * * * * *      - Every minute
#   0 * * * *      - Every hour
#   0 0 * * *      - Every day at midnight
#   0 0 * * 0      - Every Sunday at midnight
#   0 3 1 * *      - First day of month at 3 AM
#
